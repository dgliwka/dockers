version: "3.3"

services:
  whoami:
    image: containous/whoami
    container_name: whoami
    labels:
      - traefik.enable=true
      - traefik.http.routers.whoami.rule=HostRegexp(`whoami.{domain:[a-z]+}.{tld:[a-z]+}`)
    networks:
      - traefik 
networks:
  traefik:
    external: true
